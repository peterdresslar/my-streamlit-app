[project]
name = "streamlit-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.53.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.13",
]

[project-scripts]
start = "app.main:run_app"